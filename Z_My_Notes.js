//! index.js is the file that gets run when our application starts

//! JSX allows us to write our javascrip inside our HTML.

//! we use className instead of class in HTML JSX

//! every tag has to have a closing tag

//! props are the data that is passed from parent components to child components. PROPS NEVER CHANGE
